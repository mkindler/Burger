<li class="devoured-burger{{this.id}}">
    {{this.burger_name}}

    <button class="delete-btn" data-id="{{this.id}}" data-devoured="{{this.devoured}}">
        {{#if devoured}}{{else}}Clean Up{{/if}}
    </button>
</li>